# Memory index — competitor-intel-agent

One line per memory file; agents read this index first, then only the relevant files (spec Element 3). Populated by the hub after each checker-PASSed run (spec Element 14) — per-competitor files (`competitor-<slug>.md`), weekly episodic files (`episodic-<week>.md`), `sources-reliability.md`, and `procedure-scan.md` appear here after the first real run.

*(Run state lives in [state.md](state.md), not in this index.)*
